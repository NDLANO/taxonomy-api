server.port: 5000
graph:
  url: remote:example.com/database-name
  username: username
  password: secret

spring.datasource:
  url: jdbc:h2:mem:test;MODE=PostgreSQL;DB_CLOSE_ON_EXIT=FALSE
  username: sa
  password:

---

spring.profiles: dev
